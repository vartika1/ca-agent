# NEVER commit real taxpayer data — PII (PAN, salary, bank, trades, Form 16s)
clients/
*.pdf
*_form16*.txt
*_trades.json

# scratch / caches
__pycache__/
*.pyc
.DS_Store

# downloaded statements (may contain PII)
*AIS*.json
*taxpnl*.xlsx

# internal build/planning doc — kept locally, not part of the public skill
PLAN.md
